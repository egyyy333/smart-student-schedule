name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies 🔧
        run: npm install --no-audit --no-fund

      - name: Run Linter 🔍
        run: npm run lint

      - name: Build Project 🏗️
        run: npm run build

      - name: Setup Java JDK 21 ☕
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Capacitor Sync 🔄
        run: npx cap sync

      - name: Build Android App 🤖
        run: |
          cd android
          # Download a clean, official gradle-wrapper.jar directly to prevent any git text-mode corruption
          curl -sSLo gradle/wrapper/gradle-wrapper.jar https://raw.githubusercontent.com/gradle/gradle/v8.4.0/gradle/wrapper/gradle-wrapper.jar
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact 📲
        uses: actions/upload-artifact@v4
        with:
          name: smart-student-schedule-android-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload Build Artifact 📦
        uses: actions/upload-artifact@v4
        with:
          name: smart-student-schedule-build
          path: dist/

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
          commit-message: "Deploy Smart Student Schedule App [skip ci]"
